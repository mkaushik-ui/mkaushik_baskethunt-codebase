SOI Source CMS — config directory

- config.php is generated by install/index.php and must not be shipped with secrets.
- install.lock is written after successful installation and blocks the installer.
- Keep config.php permissions owner-only when possible (chmod 600).
